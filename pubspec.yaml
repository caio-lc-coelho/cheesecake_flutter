name: cheesecake
description: Teste Cheesecake Labs

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cached_network_image: ^2.5.0
  cupertino_icons: ^1.0.0
  shared_preferences: ^0.5.12+4
  url_launcher: ^5.7.10

  flutter_localizations:
    sdk: flutter   

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    #Toda a pasta de assets é importada, para evitar ter que informar cada asset manualmente
